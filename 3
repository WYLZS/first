#include<stdio.h>
int fib(int n)
{
    int arr[100000]={0};
    int i;
    arr[0]=1;
    arr[1]=1;
    for(i=2;i<=(n-1);i++)
    {
        arr[i]=arr[i-1]+arr[i-2];
    }
    i=arr[n-1];
    return i;
}
int main()
{
    int n;
    scanf("%d",&n);
    printf("%d\n",fib(n));
    return 0;
}
