#include<stdio.h>
int main()
{
    char a[100];
    char c;
    int i,b=0;
    gets(a);
    scanf("%c",&c);
    for(i=0;;i++)
    {
        if(a[i]==c)
            b++;
        if(a[i]==0)
            break;
    }
    printf("%d",b);
    return 0;
}
